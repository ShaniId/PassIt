<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.passit1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PassIt1">
        <activity android:name=".DatePicker"></activity>
        <activity android:name=".ToKnowTheClientMenu" />
        <activity android:name=".Day_1" />
        <activity android:name=".ToKnowTheClient" />
        <activity android:name=".WelcomeBack" />
        <activity android:name=".PersonalDetailsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>